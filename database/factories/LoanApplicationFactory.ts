import loanApplication from 'App/Models/loanApplication'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(loanApplication, ({ faker }) => {
  return {
    //
  }
}).build()
